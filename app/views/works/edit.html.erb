<%= render :partial => "sessions/header" if current_user %>
<% content_for :head do %>
<%= stylesheet_link_tag "header", :media=>"all" %>
<% end %>

<div id="container">

  <%= render 'form' %>

  <span class="links"><%= link_to 'Back', works_path %></span>

</div>

<%= stylesheet_link_tag "work_new.css.scss" %>
